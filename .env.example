# Copia in .env per sviluppo locale (mai committare .env)

# Database
DB_HOST=localhost
DB_NAME=registro_facciale
DB_USER=root
DB_PASS=

# App
# Genera un valore casuale lungo, es: openssl rand -hex 24
API_KEY=
BASE_URL=http://localhost/registro

# Email — From address e app password Gmail (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_USER=
SMTP_PASS=
