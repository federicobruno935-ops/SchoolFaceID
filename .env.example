# Copia in .env per sviluppo locale (mai committare .env)

# Database (Railway fornisce queste automaticamente con il plugin MySQL)
DB_HOST=localhost
DB_NAME=registro_facciale
DB_USER=root
DB_PASS=

# App
API_KEY=REDACTED_API_KEY
BASE_URL=https://tuo-progetto.up.railway.app

# Email (SMTP Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_USER=federicobruno935@gmail.com
SMTP_PASS=
